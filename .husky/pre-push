#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🧪 Running E2E tests before push..."

# Run all non-keyboard-shortcuts E2E tests
echo "Running core E2E tests..."
npm run test:e2e -- --grep-invert "keyboard-shortcuts.spec.js" --timeout=120000 --reporter=line

if [ $? -ne 0 ]; then
  echo "❌ Core E2E tests failed. Push aborted."
  echo "💡 Fix the failing tests or use --no-verify to skip this check."
  exit 1
fi

# Run only critical keyboard shortcuts tests (from keyboard-shortcuts.spec.js file)
echo "Running critical keyboard shortcuts tests..."
npm run test:e2e -- keyboard-shortcuts.spec.js --grep "@critical" --timeout=120000 --reporter=line

if [ $? -ne 0 ]; then
  echo "❌ Critical keyboard shortcuts tests failed. Push aborted."
  echo "💡 Fix the failing tests or use --no-verify to skip this check."
  exit 1
fi

echo "✅ All E2E tests passed!"
echo "💡 Run 'npm run test:e2e' to execute the full test suite including all keyboard shortcut tests"
